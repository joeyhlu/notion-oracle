/** Tool names exposed by calendar-server.ts, kept importable without loading the server itself. */
export const CALENDAR_TOOL_NAMES = ["calendar_status", "calendar_open", "calendar_open_date", "calendar_create_event"] as const;
